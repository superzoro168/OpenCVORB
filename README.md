# OpenCVORB

此範例使用OpenCV套件，並且使用ORB實作特徵比對兩張圖片相似特徵，最後會輸出一張比對結果圖在螢幕上。

## 環境

在這裡使用OpenCV 3.0.0的套件做測試

## 測試方式

1. 在外部儲存媒體(SDCard/ExternalStorage)建立一個名稱為「OpenCVORB_folder」的資料夾

2. 放入比較的兩張圖片 p01.jpg及p02.jpg

3. Run App
